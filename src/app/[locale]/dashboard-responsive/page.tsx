import { redirect } from "next/navigation";

/** Alias → Pass B home lab (`/dashboard/home-responsive`). */
export default function DashboardResponsiveAliasPage() {
  redirect("/dashboard/home-responsive");
}
