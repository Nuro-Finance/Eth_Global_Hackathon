"use client";

import { CardsGrid } from "@/features/dashboard/cards";

/** Frozen existing Agent Cards page — do not edit. Switch OFF renders this only. */
export default function AgentCardsExistingPage() {
  return <CardsGrid mode="agent" />;
}
