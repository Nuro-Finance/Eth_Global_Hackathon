// 2026-05-25: Theme toggle retired. Dark is the only theme.
// This file remains as a no-op stub so any lingering imports don't break
// the build. The visible toggle in the header was removed; settings page
// dark-mode row was removed; the legacy multi-option theme picker rendered
// by this component is dead UI.

export default function ThemeToggle() {
  return null;
}
