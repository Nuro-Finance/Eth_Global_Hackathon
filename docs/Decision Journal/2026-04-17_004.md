---
type: decision
session: 21
date: 2026-04-17
time: "17:10"
query: "Live deposit pipeline under investor-day pressure — debug and harden in one session"
decision_path: "baseline working (38 past txs on 6 chains) → user $0.10 Base deposit silently dropped → discovered monitor only polled cached deposit_addresses (not derived) → fix: derive from users table → deploy 768fbd1 → next $0.25 detected but bridge fails NETWORK_ERROR → discovered Base publicnode URL typo ('base-mainnet-rpc' vs 'base-rpc') → fix + deploy fe5c5ef → bridge succeeds (tx 0xe45af020) → repeat $0.10 test silently skipped → discovered processDeposit dedup had no time window → fix + deploy 8289573 → repeat same-amount test STILL skipped → discovered pollChain inflight check ALSO had no time window (separate dedup layer) → fix + deploy 2c3f213 → $0.17 (aggregated $0.10+$0.07) bridged successfully → admin panel 38→41 on-chain txs, volume $6.33→$6.75"
files_read:
  - "Cashly_Source_Code/src/monitor.ts"
  - "Cashly_Source_Code/src/nonce-manager.ts"
  - "Cashly_Source_Code/src/bridge.ts"
  - "Cashly_Source_Code/src/features/dashboard/my-card-1/components/ReloadFlow.tsx"
  - "Neural Net/Claude Memory/Memetropolis Intelligence.md"
  - "Neural Net/Claude Memory/Accounts & Test Users.md"
files_modified:
  - "Cashly_Source_Code/src/monitor.ts (derive-from-users + 2 dedup time windows + 1 inflight window)"
  - "Cashly_Source_Code/src/nonce-manager.ts (Base publicnode URL typo fix)"
  - "Cashly_Source_Code/src/bridge.ts (reverse CCTP param + cctpBaseToPolygon helper)"
  - "Cashly_Source_Code/src/features/dashboard/my-card-1/components/ReloadFlow.tsx (QRCode empty-string guards + vault/direct toggle)"
  - "Cashly_Source_Code/scripts/audit-schema.sql (schema drift detector)"
  - "Cashly_Source_Code/src/migrations/023_schema_migrations_tracking.sql (new)"
  - "Cashly_Source_Code/.claude/skills/gate-check/gates.yaml (schema-integrity + agent-profit-sweep-live + agent-funding-live gates)"
  - "Neural Net/Investor Prep/2026-04-17_Meeting/* (11 prep docs)"
  - "Neural Net/Sub-Agents/ (scaffold with INDEX + conflict-resolution + orchestrator + 2 new agent docs + D3 generator)"
  - "Neural Net/Audits/2026-04-17_gate-audit.md (first manual audit artifact)"
commits_today:
  - "3d334a6 feat(sprint-2.3): agent execution backend + backend CI gate"
  - "471cf2e fix: @polymarket/clob-client to deps"
  - "c578c18 chore: untrack tsconfig.tsbuildinfo + remove yarn.lock"
  - "ecfee79 fix: gate sweepAgentProfits behind AGENT_PROFIT_SWEEP_ENABLED"
  - "b8aefe6 fix: migration 021 FK agents.id is UUID on prod"
  - "cd8254c fix: add tsx to runtime dependencies"
  - "5f191e7 fix: migration 022 agent_bets settlement columns"
  - "29b416f feat(schema-integrity): structural drift prevention"
  - "693c850 fix(audit-schema): baseline types verified against production"
  - "037daa4 feat(bridge): reverse CCTP — parameterize destChainId"
  - "d689d95 fix(my-card-1): guard QRCode.toDataURL against empty string"
  - "768fbd1 fix(monitor): derive EVM vault addresses from users table"
  - "6b433cb feat(my-card-1): Base-direct vs vault route toggle"
  - "8289573 fix(monitor): time-bound dedup window to 60 min"
  - "fe5c5ef fix(nonce-manager): correct Base publicnode RPC URL"
  - "2c3f213 fix(monitor): time-bound pollChain inflight check"
confidence: 90
outcome: success
tags: [investor-day, sprint-2.3, schema-drift, dedup, rpc-url, live-debug, telemetry-gap, four-bug-cascade, monitor, bridge, on-fire]
reasoning: "Investor meeting on 1pm PST. Did Sprint 2.3 backend overnight (7 commits), then fielded live bridge failures in the prep hour. Four separate silent bugs cascaded — each individually small, all hidden by lack of telemetry on skip paths. Every bug became a one-line fix with CI-gated commit. Platform was MORE reliable at end of day than at start. Real money moved end-to-end for a live audience narrative."
learnings:
  - "Every silent skip path in a money-movement pipeline must emit observable telemetry. Richard's prod-ops instinct caught this: 'why didn't the admin console show the skipped deposit with a reason?'"
  - "Dedup logic applied across multiple layers (pollChain AND processDeposit) must share ONE helper, not duplicate logic independently. Three separate dedup implementations meant fixing one didn't fix the others."
  - "Public RPC URLs are not standardized across chains — publicnode.com uses 'base-rpc', 'ethereum-rpc', 'arbitrum-one-rpc'. Don't assume pattern; verify each."
  - "CREATE TABLE IF NOT EXISTS is unsafe for pre-existing tables with schema drift. Use ALTER TABLE ADD COLUMN IF NOT EXISTS + verify via information_schema.columns first. Structural prevention shipped same day via schema_migrations + audit-schema.sql + /gate-check schema-integrity."
  - "Live debugging under meeting pressure produces a stronger pitch than a clean rehearsal. 'Platform that survives contact with production' > 'polished demo.'"
next_steps:
  - "Session 22 HIGH-priority: consolidate 3 dedup layers into one helper"
  - "Session 22 HIGH: telemetry on every skip path (console.log + execution_log 'skipped' row)"
  - "Session 22 HIGH: FE rebuild + pm2 restart 1 so toggle + my-card-1 crash fix land in browser"
  - "Session 22 HIGH: rotate ADMIN_KEY (exposed in chat)"
  - "Session 22: wallet-prompt UX (marathon Step 8b — newly added)"
  - "Session 22: address Issuer null-balance (talk to Owen re: mangled card setup)"
  - "Session 22: pre-SSH gate-check (prevents dirty-tree merge aborts)"
verification:
  - "Admin panel 41 on-chain txs, $6.75 volume, $0.2874 fees, 6 chains"
  - "3 successful same-day bridges: 0xe45af020..., 0x18a6bb3d..., and earlier test"
  - "75 unit tests green on every CI run (16 commits, all green)"
  - "schema_migrations table on production: 23 rows, self-registering going forward"
  - "audit-schema.sql: 46/46 OK, zero drift"
---
