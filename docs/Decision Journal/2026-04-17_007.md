---
type: decision
session: 22
date: 2026-04-17
time: "23:55"
query: "Scout high-value Claude Code skills and npm libraries to adopt — ranked by AFI-specific ROI and integration cost"
decision_path: "Session 22 Hour 3 — after shipping Sprint 6.1 (silent-skip telemetry), 6.2 (dedup consolidation), and 6.3 (POLL_INTERVAL_MS env + DECIMALS_BY_CHAIN registry + dirty-tree gate + /deployer skill rewrite), spawned a claude-code-guide research agent to inventory high-value additions across two axes (Claude Code skills + npm libs). Constrained scope to picks that: (a) address pain points we hit live tonight, (b) skip anything Richard already has, (c) exclude anything requiring >1 day to integrate in a pre-investor sprint."
files_read:
  - "Research-only, no files modified. Research agent returned ranked picks."
files_modified:
  - "Neural Net/Decision Journal/2026-04-17_007.md (this file)"
confidence: 85
outcome: success
tags: [scouting, claude-code-skills, npm-libraries, sprint-planning, observability, orm, e2e-testing, marathon-6]
reasoning: "Richard wanted to know what to add next. Tonight's cascades (BSC decimals, FE dep explosion, stuck bridge) all correlate with gaps we now know specifically: no structured logging wired, no schema-drift gate, no E2E tests, raw SQL queries everywhere, no error-tracking. Agent's research surfaces actionable picks at 3 cost tiers (instant / half-day / multi-day) so Richard can match to his calendar."
ranked_picks:
  axis_1_claude_code_skills:
    - name: "Database Migration Safety Auditor"
      type: "new skill to create"
      rationale: "Auto-scans src/migrations/*.sql, compares to information_schema.columns on prod, flags unsafe FK rewrites/lock-duration risks BEFORE push. Directly addresses Session 22 Sprint 2.3 deploy pain (three serial schema drifts surfaced as migration failures). Build on existing /gate-check + scripts/verify-deps.js pattern."
      cost: "4-6 hours build"
      priority: 2
    - name: "E2E Test Generator for Playwright"
      type: "marketplace plugin"
      rationale: "Zero E2E tests today. Investor demo worked via manual testing. Slice-1a hardening needs E2E-gated deployment. Playwright chosen over Cypress (free parallelism, WebKit support, 2025 standard)."
      cost: "1-2 hours initial setup"
      priority: 3
    - name: "Observability Wiring Helper"
      type: "plugin or skill-creator-built"
      rationale: "Pino is in package.json but UNUSED. Scaffolds Pino + OpenTelemetry transport + optional Sentry sidecar. Addresses Session 21 finding: 'why didn't admin console show skip reason?' — we now have skip telemetry (Sprint 6.1) but no structured log pipeline yet."
      cost: "2-3 hours if Datadog, longer if self-hosted"
      priority: 4
  axis_2_npm_libraries:
    - name: "pino-http"
      pitch: "Express middleware auto-logging every req/res with latency. One import, one app.use() call."
      rationale: "Pino already installed. Zero-friction HTTP request tracing. Ships to stdout, PM2 picks up automatically. No config, no secrets."
      install: "npm install pino-http"
      cost: "5 minutes"
      priority: 1
      caveat: "None. Lowest-risk addition."
    - name: "@sentry/node + @sentry/tracing"
      pitch: "Drop-in error tracking. Captures unhandled rejections, integrates with Pino."
      rationale: "Zero observability above console.log today. Investor-trust table stakes. Free tier: 5K events/month."
      install: "npm install @sentry/node @sentry/tracing"
      cost: "2-3 hours (install + config + Sentry account)"
      priority: 4
      caveat: "Requires SENTRY_DSN env var (VPS .env, never git). Free tier caps at 5K/mo; filter via before-send hook if noisy."
    - name: "@tanstack/react-query"
      pitch: "Server-state caching + auto-refetch. Pairs with existing react-hook-form for mutations."
      rationale: "Redux + component state split is causing deposit-flow race conditions (likely hit during investor demo). @tanstack/react-table already installed — same org."
      install: "npm install @tanstack/react-query"
      cost: "~1 day refactor of deposit/settle flows"
      priority: 6
      caveat: "Requires QueryClient provider + converting BE endpoints to hooks. DEFER to post-funding."
    - name: "drizzle-orm + drizzle-kit"
      pitch: "Type-safe SQL builder + migration generator. Lighter than Prisma, explicit SQL, audit-safe migration files."
      rationale: "Raw pool.query() everywhere today. Doesn't scale to 10K users (no N+1 guards, no prepared statement caching). Drizzle keeps low-level SQL control that matches AFI's 'no silent schema changes' culture."
      install: "npm install drizzle-orm pg && npm install -D drizzle-kit"
      cost: "2-3 days to wrap existing tables"
      priority: 5
      caveat: "DEFER to post-funding. Risk of destabilization 48h before investor close."
recommended_sequence:
  - tier_1_instant: ["pino-http"]
  - tier_2_half_day: ["DB Migration Safety Auditor (build)", "E2E Playwright trial on /dashboard/my-card-1"]
  - tier_3_multi_day_post_funding: ["@sentry/node install", "@tanstack/react-query refactor", "drizzle-orm migration"]
synergy_notes: "DB Migration Safety Auditor + Drizzle pair: once Drizzle is in, the auditor validates every .sql file against the ORM schema. E2E Playwright + React Query together kill the deposit-race class of bug. Sentry bridges the gap between Pino (stdout logs) and investor-grade production observability."
next_steps:
  - "Session 23: consider shipping pino-http (5-min win)"
  - "Marathon 6 Sprint 6.4: scope the Database Migration Safety Auditor skill. Extends /gate-check pattern."
  - "Post-funding (Sprint 3.x): evaluate Drizzle migration as a standalone marathon"
  - "Post-funding (Sprint 3.x): React Query refactor on deposit/settle flows"
sources:
  - "Dash0 — Node.js logging 2025 comparison"
  - "Better Stack — Sentry vs Datadog 2026"
  - "Better Stack — Drizzle vs Prisma 2025"
  - "Frugal Testing — Playwright vs Cypress 2025"
  - "MCP Market — Database Migration Claude Code Skill listing"
---
