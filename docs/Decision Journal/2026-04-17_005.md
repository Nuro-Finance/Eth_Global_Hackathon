---
type: decision
session: 22
date: 2026-04-17
time: "18:00"
query: "Diagnose $40,000,000,000 BSC pending transaction on admin console and fix the underlying bug"
decision_path: "boot → scanned admin console → flagged $40B BSC row as suspected decimals-parse bug → read monitor.ts pollChain:200 → confirmed hardcoded `decimals = 6` applied to all 23 chains → read bridge.ts and identified second decimals bug in waitForArbUsdc → SSH-investigate: psql query confirmed DB row (user_id 72459d0e, user_wallet 0x75Aa3B70…, amount=40000000000, fee=2000000000, pending) → on-chain BSC check confirmed USDC decimals=18 (Binance-Peg), real balance 2e15 ($0.002 remaining from original $0.04 deposit) → math reconciled: BSC raw 4e16 formatted with 6-dec hardcode = '40000000000.0' → Arb deployer balance check showed $0.03801 (= 38010 raw 6-dec) → conclusion: LZ OFT hop SUCCEEDED, bridge stuck in waitForArbUsdc waiting for impossible 3.8e16 target → patched monitor.ts (DECIMALS_BY_CHAIN map + getChainDecimals helper used by pollChain and seedLastSeenFromDb) + bridge.ts (scale forwardAmount to Arb's 6-dec before waitForArbUsdc and cctpArbitrumToBase) + scripts/manual-sweep-arb-to-base.ts → pre-push gate passed (75 unit tests) → pushed 918c105 → VPS git pull blocked by dirty tree (POLL_INTERVAL_MS sed-edit for 60s active polling — 3rd time this session-class friction) → stash/pull/pop/restart succeeded → BUT mid-restart, new $2B pending BSC row appeared from same deposit address dust (monitor lastSeen reset, detected $0.002 dust as new deposit) → OLD code re-triggered cascade before restart → NEW code deployed but sweep script blocked by Arb deployer out-of-gas (have 0.0000046 ETH, want 0.000154 ETH) → user paused session to nap before fund+sweep completion"
files_read:
  - "Cashly_Source_Code/src/monitor.ts"
  - "Cashly_Source_Code/src/bridge.ts"
  - "Cashly_Source_Code/src/issuers.ts"
  - "Cashly_Source_Code/src/config.ts"
  - "Neural Net/Claude Memory/INDEX.md"
  - "Neural Net/Claude Memory/Pending Tasks.md"
  - "Neural Net/Claude Memory/V2 Feature Set & Marathons.md"
  - "Neural Net/Decision Journal/2026-04-17_004.md (prior session live-debug context)"
files_modified:
  - "Cashly_Source_Code/src/monitor.ts (DECIMALS_BY_CHAIN map, getChainDecimals helper, pollChain decimals fix, seedLastSeenFromDb decimals fix)"
  - "Cashly_Source_Code/src/bridge.ts (scale forwardAmount source→Arb 6-dec inside LZ two-hop path)"
  - "Cashly_Source_Code/scripts/manual-sweep-arb-to-base.ts (new — recovers stranded $0.04 from Arb deployer)"
commits_today:
  - "918c105 fix(bsc): per-chain USDC decimals — BSC uses 18-dec Binance-Peg"
confidence: 95
outcome: partial
tags: [bsc, decimals, bridge, monitor, lz-oft, layerzero, cctp, incident-response, mid-incident-deploy, vps-dirty-tree]
reasoning: "Confidence high because math reconciled end-to-end (BSC raw 4e16 → $40B display → LZ hop → Arb 38010 raw → matches). Outcome partial because sweep + DB cleanup still pending (blocked on Arb deployer funding). Root cause was architectural: shared 6-dec assumption baked into three places (monitor pollChain, monitor seedLastSeenFromDb, bridge logging) and an asymmetry in waitForArbUsdc which used source-chain decimals for an Arb-side comparison. Fix keeps per-chain decimals map since it's a static, well-known property; dynamically querying decimals() on every poll is wasteful."
learning: "BSC USDC (0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d) is Binance-Peg USDC at 18 decimals — different from all other 23 chains in CHAINS map. First live BSC deposit exposed the asymmetry. Also: LZ OFT adapters truncate via sharedDecimals when crossing chains with different decimals (BSC 18 → shared 6 on destination), so forwardAmount on source vs received on dest must be computed separately. VPS monitor.ts POLL_INTERVAL_MS sed-edit is brittle infrastructure — 3rd stash/pop cycle this session-class indicates this needs to be env-configurable rather than code-edited. Mid-restart race condition: clearing lastSeen + pending rows staying in DB can re-trigger same flow if balance still > threshold; deploy sequence should ideally mark pending rows failed BEFORE restart."
next_steps:
  - "Fund Arb deployer 0x27FbEAD2… with 0.001 ETH"
  - "Re-run manual sweep script to recover ~$0.04 and credit user 72459d0e's card"
  - "Mark both stuck DB rows (294d89f8 failed and new 5:51 pending) to reality via UPDATE"
  - "Add POLL_INTERVAL_MS env var config to remove VPS sed-edit friction (tech debt)"
  - "Add 18-dec fact to Neural Net docs (Bridge & Monitor.md)"
  - "Consider: consolidate 3 dedup layers (Session 22 priority #4) — this incident created a 4th pending row the dedup should have caught"
verification:
  - "git push pre-push: 75/75 unit tests passed on 2 test files"
  - "VPS git pull confirmed 2c3f213..918c105 Fast-forward"
  - "Stash pop restored POLL_INTERVAL_MS=60000 cleanly"
  - "PM2 restart 4 succeeded (new pid 4146358)"
  - "Deploy verified: $40B row flipped to 'failed' naturally after 20-min LZ timeout"
---
