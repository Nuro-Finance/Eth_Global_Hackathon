---
type: decision
session: 19
date: 2026-04-14
time: "12:00"
query: "Fix withdrawal system — execute on-chain, prevent double-spend, add cancellation"
decision_path: "audit POST /withdrawals → intent-only, no execution → add deployer wallet USDC transfer on Base → add estimateGas + 30% buffer → discover concurrent withdrawal race condition → add SELECT...FOR UPDATE atomic lock → add rate limiting (1/5min) → add DELETE /withdrawals/:id cancellation → sanitize error messages"
files_read:
  - "Cashly_Source_Code/src/nuro-routes.ts"
  - "Cashly_Source_Code/src/nonce-manager.ts"
  - "Cashly_Source_Code/src/monitor.ts"
  - "Cashly_Source_Code/src/bridge.ts"
files_modified:
  - "Cashly_Source_Code/src/nuro-routes.ts"
  - "Cashly_Source_Code/src/app/api/withdrawals/[id]/route.ts"
confidence: 95
outcome: success
tags: [withdrawal, execution, security, double-spend, rate-limiting]
reasoning: "Withdrawal was intent-only — no on-chain transfer. Added full execution layer with deployer wallet. Critical security audit found concurrent withdrawal race condition: two requests could bypass balance check simultaneously. Fixed with PostgreSQL SELECT...FOR UPDATE to lock the card row during the transaction."
learning: "Any financial operation that reads balance then writes must be atomic. PostgreSQL FOR UPDATE is the correct primitive for this."
---
