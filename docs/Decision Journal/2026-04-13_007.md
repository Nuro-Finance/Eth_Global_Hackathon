---
type: decision
session: 19
date: 2026-04-13
time: "09:45"
query: "Wire remaining LZ chains (Celo, Gnosis, BSC) with correct DVN addresses"
decision_path: "fetch DVN addresses from LZ metadata API → discover Gnosis uses UNIQUE infra (standard addresses are EOAs!) → discover BSC uses unique ReceiveUln302 + Executor → create LZ_INFRA registry with per-chain addresses → add 6 bidirectional connections → refactor existing connections to use LZ_INFRA constants"
files_read:
  - "Cashly_Source_Code/layerzero.config.ts"
  - "Cashly_Source_Code/src/bridge.ts"
  - "Neural Net/Claude Memory/Bridge & Monitor"
files_modified:
  - "Cashly_Source_Code/layerzero.config.ts"
confidence: 90
outcome: success
tags: [bridge, LayerZero, DVN, Gnosis, BSC, Celo]
reasoning: "Session 18 taught us that wrong DVN addresses cause InvalidWorkerId(0) errors and hours of debugging. This time we verified all addresses against the LZ metadata API BEFORE writing config. Key discovery: Gnosis and BSC do NOT use the standard SendUln302/ReceiveUln302/Executor addresses — using them would silently fail because they're EOAs (not contracts) on those chains. Created LZ_INFRA registry to centralize all per-chain addresses and prevent future address mismatches."
learning: "Never assume LZ infrastructure contracts share addresses across chains. Always verify against docs.layerzero.network/v2/deployments/chains/{chain}."
---
