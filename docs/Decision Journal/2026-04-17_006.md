---
type: decision
session: 22
date: 2026-04-17
time: "19:30"
query: "Investigate why FE rebuild fails with 'Cannot find package next-intl' and fix the underlying package.json / migration bug permanently"
decision_path: "FE rebuild attempt post-yarn→npm migration → `./node_modules/.bin/next` missing → `npm install --no-save next@15.5.14 react react-dom` as quick test → build advanced then died on next-intl missing → root-cause investigation via package.json + git history → discovered package.json has NEVER declared next/react/react-dom/next-intl/35+ other FE packages in any of Object.keys(package.json) → yarn.lock (pre-c578c18) HAD them because Yarn Berry v4 tolerates undeclared deps if they appear in the lockfile → npm strict-purge after migration exposed the latent bug → spawned Explore agent to inventory all external imports across src/{app,components,features,hooks,providers,layouts,store,lib,services,i18n} + config files → 41 runtime packages + 9 build-time packages identified → versions extracted from git-historical yarn.lock via scripts/extract-yarn-versions.js (block-parser handles aggregate keys) → package.json updated with proper dependencies/devDependencies split → built verify-deps.js gate: scans every .ts/.tsx/.js/.jsx + next.config.js + postcss.config.mjs + tailwind.config.mjs for external imports, cross-checks declared in package.json → wired into .husky/pre-push AND .github/workflows/backend-ci.yml so the failure mode is impossible to reintroduce locally OR server-side → local verify-deps run passed: 54/54 declared"
files_read:
  - "Cashly_Source_Code/package.json"
  - "Cashly_Source_Code/next.config.js"
  - "Cashly_Source_Code/postcss.config.mjs"
  - "Cashly_Source_Code/tailwind.config.mjs"
  - "Cashly_Source_Code/.husky/pre-push"
  - "Cashly_Source_Code/.github/workflows/backend-ci.yml"
  - "git show 9ff7a04:yarn.lock (pre-migration)"
  - "Cashly_Source_Code/src/app, components, features, hooks, providers, layouts, store, lib, services, i18n, auth.ts, middleware.ts (via Explore agent)"
files_modified:
  - "Cashly_Source_Code/package.json (42 new dependencies + 9 new devDependencies)"
  - "Cashly_Source_Code/scripts/verify-deps.js (new — dep-verification gate)"
  - "Cashly_Source_Code/scripts/extract-yarn-versions.js (new — one-shot tool)"
  - "Cashly_Source_Code/.husky/pre-push (added verify-deps as Gate 1)"
  - "Cashly_Source_Code/.github/workflows/backend-ci.yml (added verify-deps step)"
confidence: 95
outcome: partial
tags: [package-json, npm, yarn, migration, fe-deps, deps-gate, pre-push-hook, ci-gate, latent-bug, root-cause-fix]
reasoning: "Today's surface symptom was 'FE rebuild fails — next not installed'. The real bug was latent for months: 41 FE packages + 9 build deps were imported throughout src/ but never declared in package.json. Yarn Berry v4 masked this by installing anything that appeared in yarn.lock. When c578c18 deleted yarn.lock to switch to npm, the strict-mode npm install purged all undeclared packages — exposing the underlying misconfiguration. The quick fix (npm install --no-save) would have just hidden the bug again; the proper fix declares every used package in package.json AND adds a machine-enforced gate so it can't regress. Confidence 95 because: (a) all 54 imports verified declared, (b) gate tested locally and green, (c) versions match pre-migration yarn.lock exactly for the critical ones (next@15.5.14, react@19.2.4, react-dom@19.2.4). Outcome partial pending VPS npm install + next build + pm2 restart."
learning: "Yarn Berry v4 with nodeLinker: node-modules will silently install packages that appear in yarn.lock even if they aren't declared in any package.json. This creates a latent contract where your repo LOOKS working but the package.json is a lie. The failure mode is invisible until someone (a) runs a different package manager, (b) regenerates the lockfile from scratch, or (c) deletes the lockfile. MITIGATION: always enforce a dep-verification gate that statically scans imports vs declarations. The gate is cheap (scans src/ once per push) and catches the failure at commit time instead of 3-hour rebuild-plus-debug time. Also: NEVER migrate lockfiles without first auditing that package.json is self-sufficient."
findings:
  - "FE deps undeclared in package.json: 41 runtime (next, react, react-dom, next-intl, @radix-ui/*, @privy-io/react-auth, next-auth, wagmi, viem, framer-motion, recharts, @tanstack/*, @reduxjs/*, @dnd-kit/*, lucide-react, @tabler/icons-react, etc.) + 9 dev/build (typecheck/build tooling: @types/react, @types/react-dom, @types/qrcode, tailwindcss, postcss, @tailwindcss/postcss, @svgr/webpack)"
  - "Yarn Berry v4 silently installed them via yarn.lock despite no declaration"
  - "npm strict-purge on migration killed them all"
  - "No existing CI gate caught this because typecheck ignoreBuildErrors:true AND vitest only touches backend"
next_steps:
  - "Commit + push 918c105..latest"
  - "VPS: cd ~/nuro-finance-dashboard && git pull && npm install && ./node_modules/.bin/next build && pm2 restart 1"
  - "Verify my-card-1 QR guard + Base-direct toggle are live in browser (the commits Session 22 priority #1 was about)"
  - "Followup: migrate `rm -rf node_modules && npm ci` once to verify the lockfile is truly self-contained (a --no-save install could still leave ghost packages)"
  - "Followup: audit other config files (hardhat.config.ts, vitest.config.ts) for undeclared imports that would fail the same way"
verification:
  - "node scripts/verify-deps.js: ✓ All 54 imported packages are declared in package.json"
  - "Gate wired into .husky/pre-push as Gate 1 (before typecheck + tests)"
  - "Gate wired into backend-ci.yml as new step (before typecheck + tests)"
---
