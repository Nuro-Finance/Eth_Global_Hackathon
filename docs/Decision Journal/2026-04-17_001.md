---
type: decision
session: 21
date: 2026-04-17
time: "02:50"
query: "Deploy Sprint 2.3 backend to VPS"
decision_path: "git pull aborted (VPS had uncommitted package.json/yarn.lock) → stash + pull → tsx missing (crash loop, 19 restarts) → add tsx to runtime deps → migration 021 failed (agents.id is UUID not VARCHAR on prod) → fix FK types → deploy succeeded but reconcile throws (agent_bets.pnl column missing) → migration 022 adds settlement columns idempotently → reconcile success, last_reconciled_at populated"
files_read:
  - "Cashly_Source_Code/src/execution-dispatch.ts"
  - "Cashly_Source_Code/src/migrations/021_agent_fundings_and_sweeps.sql"
  - "Cashly_Source_Code/src/migrations/010_agents.sql"
  - "Cashly_Source_Code/package.json"
files_modified:
  - "Cashly_Source_Code/src/migrations/021_agent_fundings_and_sweeps.sql"
  - "Cashly_Source_Code/src/migrations/022_agent_bets_settlement_columns.sql"
  - "Cashly_Source_Code/package.json"
  - "Neural Net/memory/tech_afi_vps_schema_reality.md"
confidence: 95
outcome: success
tags: [deploy, migration, tsx, sprint-2.3, schema-drift, vps, hotfix]
commits:
  - "3d334a6 feat(sprint-2.3): agent execution backend + backend CI gate"
  - "471cf2e fix: @polymarket/clob-client to deps"
  - "c578c18 chore: untrack tsconfig.tsbuildinfo + remove yarn.lock"
  - "ecfee79 fix: gate sweepAgentProfits behind AGENT_PROFIT_SWEEP_ENABLED"
  - "b8aefe6 fix: migration 021 FK agents.id is UUID"
  - "cd8254c fix: tsx to runtime deps"
  - "5f191e7 fix: migration 022 agent_bets missing settlement columns"
reasoning: "Three VPS schema drifts discovered serially during the first Sprint 2.3 deploy — each CREATE TABLE IF NOT EXISTS in migration 010 silently no-op'd because target tables pre-existed with a different schema. Classic hidden-state trap. All three resolved within the same session via targeted hotfixes. Sprint 2.3 backend now live on VPS with observe-only flags default-off (no real USDC moves until Monday flip + gate-check green)."
learning: "CREATE TABLE IF NOT EXISTS is unsafe when the table may pre-exist with a different schema — it silently fails to apply intended columns. For new migrations against existing tables, always prefer ALTER TABLE ADD COLUMN IF NOT EXISTS with the actual declared type list. Before writing any FK to an existing table, query information_schema.columns to confirm actual types. Saved as tech_afi_vps_schema_reality.md in memory."
next_steps:
  - "Session 22 morning: add pre-SSH gate-check that runs 'ssh cash@vps git status --porcelain' to catch dirty-tree case before git pull aborts"
  - "Session 22 morning: backfill 15 existing skill docs into Neural Net/Sub-Agents/"
  - "Session 22: write reverse CCTP Base→Polygon function (cctpBurnAndMintReverse) — blocks agent-funding-live gate"
  - "Session 22: drop the VPS stash once verified unused (git stash drop stash@{0})"
verification:
  - "pm2 list: cashly-middleware online, restarts stable at 19"
  - "agent_fundings, agent_profit_sweeps tables exist on VPS"
  - "agents.last_reconciled_at = 2026-04-17 06:49:48 (reconcile success)"
  - "agents.last_pnl_drift_usd = 0 (no drift, expected on unfunded agent)"
  - "Backend CI green on Build_Branch HEAD 5f191e7"
---
